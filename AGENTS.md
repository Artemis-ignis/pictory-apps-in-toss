# 마스터 전용 픽토리 복구 지침

- 모든 보고는 한국어로 하고 사용자를 `마스터`라고 부른다.
- Apps in Toss 설정은 `granite.config.ts`를 기준으로 유지한다.
- 실제 사진 원본은 외부로 보내지 않는다. 분류는 브라우저/토스 WebView 안에서 처리한다.
- 실제 앨범 삭제 API가 없는 상태에서 삭제 완료처럼 표현하지 않는다. 앱 내부 정리 후보/보관 상태로 표현한다.
- 구현 후 `npm run test`, `npm run lint`, `npm run build` 또는 실패 원인을 보고한다.
